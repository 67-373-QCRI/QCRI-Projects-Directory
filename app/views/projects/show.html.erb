<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css" rel="stylesheet">
<div class="container primary-content">
	<!-- PRIMARY CONTENT HEADING -->
	<div class="primary-content-heading clearfix">
    <div class="row">
      <div class="col-2">
    		<h2><%= @project.name %></h2>
      </div>
      <div class="col align-self-center">
        <div class="dropdown">
          <% if logged_in? && current_user.admin? %>
            <i class="material-icons" role="button" data-bs-toggle="dropdown" aria-expanded="false" >tune</i>
            <ul class="dropdown-menu">
              <li><%= link_to 'Edit Project', edit_project_path(@project), class: "dropdown-item" %></li>
              <li><%= link_to 'Delete Project', project_path(@project), method: :delete, data: {confirm: "Are you sure you want to delete this project?"}, class: "dropdown-item" %></li>
            </ul>
          <% end %>
        </div>
      </div>
    </div>
        <hr style="border:1px solid #fff">
    <img src="<%= rails_blob_path(@project.image, disposition: "attachment", only_path: true) %>" class="img-thumbnail" alt="..." style="width:50%;height:50%">

	</div>
	<!-- END PRIMARY CONTENT HEADING -->
	<div class="row">
		<div class="col-md-8">
			<div class="project-section general-info">
				<h3>General Info</h3>
				<p><%= @project.description %></p>
				<div class="row">
					<div class="col-sm-9">
						<dl class="dl-horizontal">
							<dt>Start Date:</dt>
							<dd><%= @project.start_date %></dd>
							<dt>End Date:</dt>
							<dd><%= @project.end_date.nil? ? "TBA" : @project.end_date %></dd>
							<dt>Status:</dt>
							<dd><%= @project.status %></dd>
							<dt>Leader:</dt>
							  <dd>
                  <div class="card" style="width: 14rem;">
                    <img src="<%= rails_blob_path(@project.team_leader.avatar, disposition: "attachment", only_path: true) %>" class="card-img-top" alt="...">
                    <div class="card-body">
                      <h5 class="card-title"><%= @project.leader_name %></h5>
                      <p class="card-text"><%= @project.team_leader.job_title %></p>
                      <a href="<%= researcher_path(@project.team_leader_id) %>" class="btn btn-primary">More Info</a>
                    </div>
                  </div>
                </dd>
							<dt>Team:</dt>
							<dd>
								<div class="row">
                  <div class="container-fluid py-2">
                    <div class="d-flex flex-row flex-nowrap">
                      <% @project.members.each do |member| %>
                        <% unless member == @project.team_leader_id %>
                          <div class="card" style="width: 14rem;">
                            <img src="<%= rails_blob_path(Researcher.find(member).avatar, disposition: "attachment", only_path: true) %>" class="card-img-top" alt="...">
                            <div class="card-body">
                              <h5 class="card-title"><%= Researcher.find(member).full_name %></h5>
                              <p class="card-text"><%= Researcher.find(member).job_title %></p>
                              <a href="<%= researcher_path(member) %>" class="btn btn-primary">More Info</a>
                            </div>
                          </div>
                        <% end %>
                      <% end %>
                    </div>
                  </div>
								</div>
							</dd>
						</dl>
					</div>
				</div>
			</div>
		</div>
		<div class="col-md-4">
			<!-- Publications -->
      <% if @project.publications.count > 0 %>
        <% @project.publications.each do |publication| %>
          <div class="card" style="width: 36rem;">
            <div class="card-body">
              <h5 class="card-title"><%= publication.title %></h5>
              <h6 class="card-subtitle mb-2 text-muted"><%= publication.authors_citation %></h6>
              <h6 class="card-subtitle mb-2 text-muted"><%= publication.conference %>, <%= publication.pages %></h6>
              <a href="<%= publication.doi_to_url %>" class="btn btn-outline-primary">Go to</a>
            </div>
          </div>
        <% end %>
      <% end %>
			<!-- END Publications -->
		</div>
	</div>
</div>
