<nav class="navbar navbar-expand-lg navbar-dark" style="background-color:#1082e0">
  <div class="container-fluid">
    <a class="navbar-brand d-flex" href="<%= home_path %>">
      <img src="<%= asset_path('logos/qcri-logo.png')%>" alt="Logo" width="60" height="60" class="d-inline-block align-text-center">
      <span class="ml-3">Qatar Computing Research Institute<br><small>Projects Directory</small></span>
    </a>
    <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="<%= nav_is_active('projects') %>" aria-current="<%= indicate_current('projects') %>" href="<%= projects_path %>">Projects</a>
        </li>
        <li class="nav-item">
          <a class="<%= nav_is_active('researchers') %>" aria-current="<%= indicate_current('researchers') %>" href="<%= researchers_path %>">Researchers</a>
        </li>
        <li class="nav-item">
          <a class="<%= nav_is_active('products') %>" aria-current="<%= indicate_current('products') %>" href="<%= products_path %>">Products</a>
        </li>
      </ul>
      <ul class="nav navbar-nav mr-auto">
        <% if logged_in? %>
        <div class="btn-group">
          <% if current_user.researcher.nil? %>
            <a class="btn btn-light navbar-btn" href="<%= new_researcher_path %>">Create Your Researcher Page</a>
          <% else %>
            <a class="btn btn-light navbar-btn" href="<%= edit_researcher_path(current_user.researcher) %>">Edit Your Researcher Page</a>
          <% end %>
          <a class="btn btn-secondary navbar-btn" href="<%= logout_path %>">Logout</a>
        </div>
        <% else %>
          <div class="btn-group">
            <a class="btn btn-light navbar-btn" href="<%= login_path %>">Log In</a>
            <a class="btn btn-secondary navbar-btn" href="<%= signup_path %>">Sign Up</a>
          </div>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
