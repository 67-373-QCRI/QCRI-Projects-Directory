<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= home_path %>">
      <img src="<%= asset_path('logos/qcri-logo.jpg')%>" alt="Logo" width="60" height="60" class="d-inline-block align-text-center">
      Qatar Computing Research Institute Catalog
    </a>
    <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="<%= nav_is_active('projects') %>" aria-current="<%= indicate_current('projects') %>" href="<%= projects_path %>">Projects</a>
        </li>
        <li class="nav-item">
          <a class="<%= nav_is_active('researchers') %>" aria-current="<%= indicate_current('researchers') %>" href="<%= researchers_path %>">Researchers</a>
        </li>
        <li class="nav-item">
          <a class="<%= nav_is_active('products') %>" aria-current="<%= indicate_current('products') %>" href="<%= products_path %>">Products</a>
        </li>
      </ul>
      <ul class="nav navbar-nav mr-auto">
        <% if logged_in? %>
          <a class="nav-link" href="<%= logout_path %>">Logout</a>
        <% else %>
          <div class="btn-group">
            <a class="btn btn-outline-primary navbar-btn" href="<%= login_path %>">Log In</a>
            <a class="btn btn-outline-success navbar-btn" href="<%= signup_path %>">Sign Up</a>
          </div>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
